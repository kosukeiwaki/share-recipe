.header
.main
  .postBox
    .contents.row
      .container
        -# = form_with(model: @recipe, local: true) do |f|
        -#   %h3
        -#     編集する
        -#   = f.text_field :name, placeholder: "なまえ"
        -#   = f.text_field :title, placeholder: "料理名"
        -#   = f.text_field :image, placeholder: "画像"
        -#   = f.text_area :text, placeholder: "思い出" , rows: "10"
        -#   = f.submit "編集する"
